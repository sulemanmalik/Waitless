import React from "react";
import { Grid } from "@material-ui/core";

const Bookings = props => {
  return (
    <Grid container justify="center" style={{ marginTop: 100 }}>
      <h1>Bookings pagde</h1>
    </Grid>
  );
};

export default Bookings;
